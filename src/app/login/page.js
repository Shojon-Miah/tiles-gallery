import LoginForm from "@/components/auth/LoginForm";

export const metadata = {
  title: "Login — Tiles Gallery",
  description: "Log in to your Tiles Gallery account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
