import RegisterForm from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Register — Tiles Gallery",
  description: "Create a new Tiles Gallery account.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
